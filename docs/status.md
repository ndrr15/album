# Estado actual y próximos pasos

Estado detectado:
- Migraciones: `database/migrations/001_initial_schema.sql` y `supabase/migrations/20260601154054_initial_schema.sql`.
- No se detectó un `backend/README.md` ni archivos de arranque en `backend/`.
- `mobile/` existe pero sin documentación pública en `docs/`.

Próximos pasos sugeridos:
1. Definir y documentar el stack del backend.
2. Añadir instrucciones de arranque y variables de entorno en `backend/README.md`.
3. Sincronizar migraciones entre `database/` y `supabase/` o documentar diferencias.
4. Añadir un `CONTRIBUTING.md` y `CODE_OF_CONDUCT.md` si el proyecto será colaborativo.
